version: "3"

services:
  database:
    image: mysql:8.0
    container_name: mysql
    ports:
      - "3307:3306"
    volumes:
      - ./mysql_data:/var/lib/mysql
    environment: 
      MYSQL_ROOT_PASSWORD: "kimgangmin7259"
      MYSQL_DATABASE: "whether_etl"

